node_modules/
docs/plans/
*.js.map
*.tgz
.env
.env.*
.DS_Store
*.log
npm-debug.log*
coverage/
*.tsbuildinfo
spike/
vertex-models.json

# IDE / Editor folders
.vscode/
.idea/
.cursor/

# Local agent planning / logs. `.claude/skills` and `.claude/docs` are committed —
# they are the agent-facing skills and subsystem references (see CLAUDE.md).
.claude/*
!.claude/docs/
!.claude/skills/
!.claude/harnesses/
.gemini/
.planning/
ANTIGRAVITY-DEBUG-SESSION.md

# Personal model alias config for the custom-model patcher (edit the example)
scripts/patch-custom-models/model-config.json

# build output — rebuilt by prepublishOnly and CI before publish
dist/

# probe bundle built on the fly by tests/parent-notice-launch.test.ts
tests/helpers/*.built.mjs
